# 项目工作规则

## 项目概况

- 项目名称：PPT Agent
- 项目目标：提供可独立部署、可恢复、可嵌入不同宿主系统的 PPT 智能体。
- 首个宿主：FrameFlow。
- 后续宿主：ShanHaiEdu 课件系统。
- 技术栈：TypeScript、Bun；HTTP、SSE、数据库和 UI 通过适配层接入。

## 服务器路径

- 阿里云开发仓库：`/srv/codex-workspace/PPT-Agent`，使用 `codex-dev` 开发、测试和构建。
- 当前验收部署根：`/opt/ppt-agent`，当前版本由 `/opt/ppt-agent/current` 软链接确定；它目前是董事长个人验收站，不是对外正式生产环境。
- 隔离测试根：`/opt/ppt-agent-test`，当前版本由 `/opt/ppt-agent-test/current` 软链接确定。
- `/opt` 内 release、共享数据、环境文件和备份不是开发工作树，不得反向覆盖源码。
- README 当前可能包含进行中的用户改动；修改前必须先检查 Git 状态并保留现场。

## 当前部署模式：内部验收快速部署

> 自 2026-08-06 起，`/opt/ppt-agent` 与其 FrameFlow 接入仅用于董事长个人验收。该模式持续有效，直到董事长在新的明确指令中写出“切换为正式对外生产”。`/opt` 路径、release、`current`、端口或公网可达都不是生产模式的隐含授权。

> 本节在本模式有效期内，覆盖继承规则中仅适用于正式对外生产的默认流程，包括发布前备份、回退演练、全量门禁、发布治理、灰度和长期监控。它不覆盖平台限制，也不放宽密钥、认证、回环端口、真实付费调用、公共合同正确性、迁移历史或删除范围的约束。

### 快速验收规则

- 优先让董事长尽快验证 V1/V2 API、任务状态和受影响 UI；已提交且通过本轮相关验证的改动可直接构建、切换或重启验收服务。
- 默认不创建数据库/产物/配置备份，不做恢复演练、发布治理、灰度、长期监控或全量无关测试。只有董事长明确要求保存数据或切换正式对外生产时才执行。
- 仅运行构建、本轮相关测试或类型检查，以及 health、认证边界和受影响 API 的最小 smoke；本轮改动导致的失败必须修复，无关既有失败只记录，不扩展为发布阻塞项。
- 当前数据库、Run、步骤、事件、产物和测试备份均可视为可丢弃验收数据。董事长明确要求重置时，可在停止相关服务后直接清空指定数据与产物路径，不必先创建备份；执行前须列明绝对路径，确保不触及源码、release、环境文件、凭据或其他项目。
- 继续保留源码、release、环境文件、密钥和服务拓扑，除非董事长明确授权删除；不得因清空验收数据删除其他项目或 Git 历史。

### 不可放宽的边界

- 不输出、提交或复制 Token、`.env` 正文、Cookie、私钥或用户隐私数据。
- 服务继续仅绑定回环地址；不得开放新公网端口、绕过认证、修改安全组或启动全局代理/TUN。
- 默认测试必须使用 Mock、隔离 runtime、只读请求或非计费接口。真实 Provider 调用仍需董事长当次明确授权。
- 公共合同、幂等、计费状态和跨宿主边界的变更，仍必须有对应回归测试；快速部署不允许以破坏合同换取速度。
- 删除前先列出绝对路径；董事长授权清空当前验收数据时，可跳过备份，但不能扩大到源码、release、环境文件、凭据或其他项目目录。

### 快速部署步骤

1. 确认本轮提交、候选构建和工作区状态，只处理本轮范围。
2. 运行构建和直接相关的质量检查；本轮改动导致的失败必须修复，无关既有失败只记录，不追加无关发布流程。
3. 如需数据库结构更新，执行项目已有迁移工具；无需默认备份，不得以会偏离迁移历史的重置命令替代正常迁移，除非董事长明确要求重置结构。
4. 切换候选 release 或直接重启受影响服务，不额外制作发布备份；核对 health、回环端口和一个受影响 API 路径。
5. 涉及 UI 或 FrameFlow 接入时，用真实浏览器检查桌面和移动端受影响页面或流程；不需要完成对外生产级全链路验收。

### 正式生产切换条件

- 只有董事长在新的任务中明确写出“切换为正式对外生产”，才恢复生产数据保护、可恢复备份、回退、完整发布门禁、部署记录和发布后监控。
- 在此前，任何任务不得把“上线”“正式目录”或“公网可访问”自行解释成正式对外生产。

## 边界

- `src/core/` 不得导入 FrameFlow、ShanHaiEdu、Next.js、Prisma 或具体 Provider SDK。
- `src/contracts.ts` 是宿主与运行时共享的稳定公共合同。
- 宿主身份使用 `tenantId`、`externalUserId` 和 `externalProjectId`，不得保存宿主 Cookie 或会话。
- 计费媒体调用必须携带稳定幂等键；未知提交状态禁止自动换 Key 重试。
- 测试默认只使用内存适配器和 Mock Provider，禁止调用真实计费模型。
- 新增外部依赖前说明用途并优先保持核心包无框架依赖。

## 命令

- 安装：`bun install --frozen-lockfile`
- 测试：`bun test`
- 类型检查：`bun run typecheck`
- 构建：`bun run build`

## 完成标准

- 公共合同、状态转换、预算和幂等行为有自动化测试。
- 核心层独立性检查不允许出现宿主或框架导入。
- FrameFlow 接入必须由功能开关保护，关闭后原 PPT 流程无行为变化。
- 当前内部验收部署和已界定验收数据重置可按本模式常规执行；数据库结构变更仍使用已有迁移工具。真实 Provider 请求必须另行明确授权。只有明确切换为正式对外生产时，才恢复完整生产部署、备份和回退授权要求。
